const mongoose = require('mongoose');
const User = require('../src/user');
const Comment = require('../src/comment');
const BlogPost = require('../src/blog-post');

describe('Associations', () => {
    xit('should relate 2 models', (done) => {
        done();
    });
});